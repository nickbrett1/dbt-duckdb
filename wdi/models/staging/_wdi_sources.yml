version: 2

sources:
  - name: wdi
    database: "{{ 'wdi' if target.name == 'dev' else target.database }}"
    schema: "{{ 'public' if target.name == 'dev' else target.schema }}"
    description: "Raw data from WDI"

    tables:
      - name: WDIfootnote
      - name: WDICountry
      - name: WDICSV
      - name: WDIseriestime
      - name: WDISeries
      - name: WDIcountryseries
      - name: population_data
